extends Node3D

@export_range(0.0, 1.0) var dayhour_normalized: float = 0.0
#@export var daylight_color: Gradient
#@export var skylight_curve: Curve
#@export var sky_top_color: Gradient
#@export var horizon_color: Gradient
#@export var sky_bottom_color: Gradient

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
